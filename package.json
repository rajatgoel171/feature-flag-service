{
  "name": "feature-flag-service",
  "version": "1.0.0",
  "description": "Deterministic feature flag evaluation with percentage rollouts and targeting rules.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.js"
  },
  "engines": {
    "node": ">=20"
  }
}
